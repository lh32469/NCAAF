apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: portfolio-app
  name: portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-app
  strategy: { }
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: portfolio-app
    spec:
      hostAliases:
        - ip: "192.168.5.220"
          hostnames:
            - "dell-4290.local"
      containers:
        - image: lh32469/portfolio-BRANCH:BUILD
          name: portfolio
          env:
            - name: SERVER_SERVLET_CONTEXT_PATH
              value: "/"
          resources:
            limits:
              cpu: "1.25"
            requests:
              cpu: ".25"
          livenessProbe:
            httpGet:
              path: /ping
              port: 8085
              httpHeaders:
                - name: Custom-Header
                  value: Awesome
            initialDelaySeconds: 15
            periodSeconds: 5

---

kind: Service
apiVersion: v1
metadata:
  name: portfolio-service
spec:
  selector:
    app: portfolio-app
  ports:
    - port: 8085 # Default port for image

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: portfolio-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: HOSTNAME.gpc4j.org
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: portfolio-service
                port:
                  number: 8085

